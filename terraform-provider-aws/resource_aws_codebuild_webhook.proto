syntax "proto3";

package aws;

message AwsCodebuildWebhook {
  branch_filter = 1;
  id = 2;
  payload_url = 3;
  project_name = 4;
  secret = 5;
  url = 6;
  message FilterGroup {
    message Filter {
      exclude_matched_pattern = 1;
      pattern = 2;
      type = 3;
    }
    filter = 1;
  }
  filter_group = 7;
}