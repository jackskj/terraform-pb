syntax "proto3";

package google;

message GoogleContainerAnalysisNote {
  id = 1;

  // The name of the note.
  name = 2;
  project = 3;
  message AttestationAuthority {
    message Hint {

      // The human readable name of this Attestation Authority, for
example "qa".
      human_readable_name = 1;
    }
    hint = 1;
  }
  attestation_authority = 4;
  message Timeouts {
    create = 1;
    delete = 2;
    update = 3;
  }
  timeouts = 5;
}